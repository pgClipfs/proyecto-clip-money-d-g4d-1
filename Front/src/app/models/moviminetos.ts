export interface IMovi {
  ApellidoDestino: string;
  Apellido: string;
  monto: number;
  fecha: Date;
  numComprobante: number;
  movimiento: string;
}
export interface InewMov {
  idTipoMov: number;

  monto: number;
}
